// src/controllers/admin/auth.controller.ts

import { Request, Response } from 'express';
export const adminLogin = async (_req: Request, res: Response) => {
  res.status(501).json({ error: 'Not implemented yet' });
};