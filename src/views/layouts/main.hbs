{{!-- src/views/layouts/main.hbs --}}

<!DOCTYPE html>
<html lang="{{locale}}">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="referrer" content="strict-origin-when-cross-origin">
    <meta name="theme-color" content="#0057ff">
    <title>{{title}}</title>
    <meta name="description" content="{{description}}">

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300..700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/fontawesome.min.css">
    <link rel="stylesheet" href="/css/base.css">
    <link rel="stylesheet" href="/css/header.css">
    <link rel="stylesheet" href="/css/footer.css">
    <link rel="stylesheet" href="/css/home.css">
    <link rel="stylesheet" href="/css/auth.css">
    <link rel="stylesheet" href="/css/responsive.css">

    {{#each hreflangs}}
    <link rel="alternate" hreflang="{{this.lang}}" href="{{this.url}}">
    {{/each}}

    {{{_sections.head}}}
</head>

<body>
    <a class="skip-link" href="#main-content">Aller au contenu principal</a>

    {{> header}}

    <main id="main-content">
        {{{body}}}
    </main>

    {{> footer}}

    <div id="modal-container" class="modal-container">
        <div class="language-modal-overlay">
            <div class="language-modal-content">
                <div class="language-modal-icon">
                    <span class="language-flag" id="language-modal-flag">🇫🇷</span>
                </div>
                <h3 id="language-modal-title">Langue modifiée</h3>
                <p id="language-modal-text">
                    L'interface a été changée vers <strong id="language-modal-name">Français</strong>
                </p>
                <button class="language-modal-btn" id="language-modal-btn">
                    <i class="fas fa-check"></i> Parfait !
                </button>
            </div>
        </div>
    </div>

    <script type="module" src="/app.js"></script>
    {{{_sections.scripts}}}
</body>
</html>